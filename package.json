{
  "name": "soporte-tecnico",
  "version": "1.0.0",
  "description": "",
  "main": "bot.js",
  "scripts": {
    "start": "node bot.js",
    "postinstall": "apt-get update && apt-get install -y chromium-browser && ln -sf /usr/bin/chromium-browser /usr/bin/chromium || true",
    "start-proxy": "node proxy.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.13.3",
    "express": "^4.18.2",
    "puppeteer": "^22.0.0",
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.34.6"
  }
}